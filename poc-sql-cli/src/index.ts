async function main() {
  console.log("🚀 Rodando ETL (extract → transform → load) em lotes...");
  try {
    throw new Error("Função não implementada");
    process.exit(0);
  } catch (err) {
    console.error("❌ Erro no ETL:", err);
    process.exit(1);
  }
}

main();
